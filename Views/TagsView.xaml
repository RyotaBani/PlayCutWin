<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Border Background="#1A1A1A" CornerRadius="10" BorderBrush="#2A2A2A" BorderThickness="1" Padding="16">
        <StackPanel>
            <TextBlock Text="Tags" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Text="ここにタグ管理（追加/編集/削除/プリセット）を実装していく"
                       Foreground="#B0B0B0" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>
