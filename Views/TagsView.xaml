<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#111111">

    <Grid Margin="0">
        <Border Background="#1A1A1A" CornerRadius="10" BorderBrush="#2A2A2A" BorderThickness="1" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Tags" Foreground="White" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>

                <Border Grid.Row="1" Background="#151515" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Selected video:" Foreground="#BFBFBF" FontSize="12"/>
                        <TextBlock x:Name="SelectedVideoText"
                                   Text="(none)"
                                   Foreground="#EDEDED"
                                   FontSize="13"
                                   TextWrapping="Wrap"
                                   Margin="0,6,0,0"/>

                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="Time:" Foreground="#BFBFBF" FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TimeText"
                                       Text="00:00"
                                       Foreground="#EDEDED"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="(Clipsで再生中の時刻が反映されます)" Foreground="#6F6F6F" FontSize="12" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="2" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="160"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TagInput"
                             Height="32"
                             VerticalContentAlignment="Center"
                             Background="#111111"
                             Foreground="#EDEDED"
                             BorderBrush="#2A2A2A"
                             BorderThickness="1"
                             Padding="10,0"
                             Text=""/>

                    <Button Grid.Column="1"
                            x:Name="AddTagButton"
                            Content="Add Tag"
                            Height="32"
                            Margin="10,0,0,0"
                            Click="AddTag_Click"/>
                </Grid>

                <Border Grid.Row="3" Background="#151515" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="8" Padding="12">
                    <DockPanel LastChildFill="True">
                        <TextBlock DockPanel.Dock="Top"
                                   Text="Tag list"
                                   Foreground="#BFBFBF"
                                   FontSize="12"
                                   Margin="0,0,0,8"/>

                        <ListView x:Name="TagList" Background="#111111" Foreground="#EDEDED" BorderBrush="#2A2A2A" BorderThickness="1">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Time" Width="80" DisplayMemberBinding="{Binding Time}" />
                                    <GridViewColumn Header="Tag" Width="260" DisplayMemberBinding="{Binding Text}" />
                                    <GridViewColumn Header="Video" Width="420" DisplayMemberBinding="{Binding VideoPath}" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
