<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tags" Foreground="White" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <Border Grid.Row="1" Background="#151515" CornerRadius="10" BorderBrush="#2A2A2A" BorderThickness="1" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Selected video:" Foreground="#BFBFBF" FontSize="12"/>
                <TextBlock Grid.Row="1" Text="{Binding SelectedVideoPath}" Foreground="#EDEDED" FontSize="12"
                           TextWrapping="Wrap" Margin="0,4,0,12"/>

                <TextBlock Grid.Row="2" Text="(placeholder) ここにタグUIを作る" Foreground="#AFAFAF" FontSize="12" Margin="0,0,0,8"/>

                <Grid Grid.Row="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="TagInput" Height="28" VerticalContentAlignment="Center"
                                 KeyDown="TagInput_KeyDown"/>
                        <Button Grid.Column="1" Content="Add Tag (dummy)" Padding="18,6" Margin="10,0,0,0"
                                Click="AddTag_Click"/>
                    </Grid>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Tags}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="100"/>
                            <DataGridTextColumn Header="Tag"  Binding="{Binding Text}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
