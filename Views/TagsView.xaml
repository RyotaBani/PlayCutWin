<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tags"
                   Foreground="#EEE"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding SelectedVideoText}"
                   Foreground="#AAA"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="TagInput"
                     Width="220"
                     Margin="0,0,8,0"
                     KeyDown="TagInput_KeyDown"/>
            <Button Content="Add" Width="80" Margin="0,0,8,0" Click="Add_Click"/>
            <Button Content="Clear" Width="80" Click="Clear_Click"/>
        </StackPanel>

        <ListBox Grid.Row="3"
                 x:Name="TagsList"
                 ItemsSource="{Binding CurrentTags}"
                 Background="#111"
                 BorderBrush="#333"
                 BorderThickness="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="6">
                        <TextBlock Text="{Binding TimeText}" Foreground="#888" Width="70"/>
                        <TextBlock Text="{Binding Text}" Foreground="#EEE" Margin="8,0,0,0"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="4"
                   Text="Double click a tag to remove."
                   Foreground="#777"
                   Margin="0,10,0,0"/>
    </Grid>
</UserControl>
