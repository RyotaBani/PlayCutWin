<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#1c1c1c" CornerRadius="12" Padding="14">
            <StackPanel>
                <TextBlock Text="Selected video:" Foreground="#bdbdbd"/>
                <TextBlock x:Name="SelectedVideoText" Text="(none)" Foreground="White" TextWrapping="Wrap" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#1c1c1c" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="170"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TagInput"
                         Height="32"
                         VerticalContentAlignment="Center"
                         KeyDown="TagInput_KeyDown"
                         ToolTip="Type tag and press Enter"/>
                <Button Grid.Column="2" Content="Add Tag" Height="32" Click="AddTag_Click"/>
                <Button Grid.Column="4" Content="Clear (selected)" Height="32" Click="ClearTagsForSelected_Click"/>
            </Grid>
        </Border>

        <!-- Presets -->
        <WrapPanel Grid.Row="4" Margin="0,0,0,10">
            <Button Content="Transition" Tag="Transition" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="Set" Tag="Set" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="PnR" Tag="PnR" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="BLOB" Tag="BLOB" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="SLOB" Tag="SLOB" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="vs M/M" Tag="vs M/M" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="Zone" Tag="Zone" Margin="0,0,8,8" Click="Preset_Click"/>
            <Button Content="Rebound" Tag="Rebound" Margin="0,0,8,8" Click="Preset_Click"/>
        </WrapPanel>

        <Border Grid.Row="4" Background="#1c1c1c" CornerRadius="12" Padding="14" Margin="0,46,0,0">
            <DataGrid x:Name="TagsList"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="120"/>
                    <DataGridTextColumn Header="Tag" Binding="{Binding Text}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
