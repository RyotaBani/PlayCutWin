<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Tags" FontSize="22" FontWeight="SemiBold" Foreground="White"/>
        </StackPanel>

        <!-- Selected info + input -->
        <Border Grid.Row="1"
                Background="#1A1A1A"
                BorderBrush="#2A2A2A"
                BorderThickness="1"
                CornerRadius="10"
                Padding="14"
                Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Selected video:" Foreground="#B0B0B0" FontSize="12"/>

                <TextBlock x:Name="SelectedVideoText"
                           Grid.Row="1"
                           Text="(none)"
                           Foreground="White"
                           FontSize="13"
                           TextWrapping="Wrap"
                           Margin="0,6,0,10"/>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TagInput"
                             Height="30"
                             Margin="0,0,10,0"
                             VerticalContentAlignment="Center"
                             KeyDown="TagInput_KeyDown"
                             ToolTip="Type tag and press Enter" />

                    <Button Grid.Column="1"
                            Content="Add Tag"
                            Height="30"
                            Padding="14,0"
                            Margin="0,0,10,0"
                            Click="AddTag_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="30"
                            Padding="14,0"
                            Click="ClearTags_Click"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Tag list -->
        <Border Grid.Row="2"
                Background="#1A1A1A"
                BorderBrush="#2A2A2A"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,10">
                    <TextBlock Text="Tag List" Foreground="White" FontWeight="SemiBold" />
                    <TextBlock x:Name="CountText"
                               DockPanel.Dock="Right"
                               Text="Count: 0"
                               Foreground="#B0B0B0"/>
                </DockPanel>

                <ListView x:Name="TagsList" Grid.Row="1">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Time" Width="80" DisplayMemberBinding="{Binding TimeText}" />
                            <GridViewColumn Header="Tag"  Width="380" DisplayMemberBinding="{Binding Tag}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
