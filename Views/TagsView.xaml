<UserControl x:Class="PlayCutWin.Views.TagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="16">
        <Border CornerRadius="10" BorderBrush="#2A2A2A" BorderThickness="1" Padding="16" Background="#121212">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Tags" FontSize="28" FontWeight="SemiBold" Foreground="White" />

                <StackPanel Grid.Row="1" Margin="0,12,0,0">
                    <TextBlock Text="Selected video:" Foreground="#BFBFBF" />
                    <TextBlock x:Name="SelectedVideoText"
                               Text="{Binding SelectedVideoText}"
                               Foreground="White"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Grid Grid.Row="2" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TagInput"
                             Height="32"
                             Padding="8,6"
                             VerticalContentAlignment="Center"
                             Background="#0E0E0E"
                             Foreground="White"
                             BorderBrush="#2A2A2A"
                             BorderThickness="1"
                             KeyDown="TagInput_KeyDown"
                             />

                    <Button Grid.Column="1"
                            Margin="10,0,0,0"
                            Padding="14,6"
                            Content="Add (Enter)"
                            Click="AddTag_Click"/>

                    <Button Grid.Column="2"
                            Margin="10,0,0,0"
                            Padding="14,6"
                            Content="Clear"
                            Click="ClearTags_Click"/>
                </Grid>

                <Border Grid.Row="3" Margin="0,16,0,0"
                        CornerRadius="10"
                        BorderBrush="#2A2A2A" BorderThickness="1"
                        Background="#0F0F0F"
                        Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Tags for selected video"
                                   Foreground="#BFBFBF"
                                   Margin="2,0,0,8"/>

                        <ListBox x:Name="TagsList"
                                 Grid.Row="1"
                                 Background="#0F0F0F"
                                 BorderThickness="0"
                                 Foreground="White"
                                 ItemsSource="{Binding TagsForSelected}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="8"
                                            BorderBrush="#2A2A2A"
                                            BorderThickness="1"
                                            Background="#121212"
                                            Padding="10"
                                            Margin="0,0,0,8">
                                        <DockPanel>
                                            <TextBlock Text="{Binding TimeText}"
                                                       Foreground="#9A9A9A"
                                                       Width="70"
                                                       Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding Text}"
                                                       Foreground="White"
                                                       TextWrapping="Wrap"/>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
