<UserControl x:Class="PlayCutWin.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="900" d:DesignHeight="600"><Border Background="#2B2B2B"
            BorderBrush="#3A3A3A"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Vertical">
                <!-- ファイル名は小さめ + 折り返し + 省略 -->
                <TextBlock x:Name="VideoTitleText"
                           Style="{StaticResource PanelHeader}"
                           Text="Video (16:9)"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis"
                           MaxHeight="44"
                           ToolTip="{Binding ElementName=VideoTitleText, Path=Text}"/>
            </StackPanel>

            <!-- Team inputs (左上領域の “横幅1/2” だけ使う) -->
            <Grid Grid.Row="1" Margin="0,12,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Style="{StaticResource SubLabel}" Text="Team A" Margin="2,0,0,6"/>
                        <TextBox x:Name="TeamATextBox"
                                 Style="{StaticResource WatermarkTextBox}"
                                 Tag="Home / Our Team"
                                 TextChanged="TeamATextBox_TextChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Style="{StaticResource SubLabel}" Text="Team B" Margin="2,0,0,6"/>
                        <TextBox x:Name="TeamBTextBox"
                                 Style="{StaticResource WatermarkTextBox}"
                                 Tag="Away / Opponent"
                                 TextChanged="TeamBTextBox_TextChanged"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- Video area (MediaElement) -->
            <Border Grid.Row="2"
                    Background="#3A3A3A"
                    CornerRadius="12"
                    ClipToBounds="True">

                <Grid>
                    <MediaElement x:Name="Player"
                                  LoadedBehavior="Manual"
                                  UnloadedBehavior="Manual"
                                  Stretch="Uniform"
                                  MediaOpened="Player_MediaOpened"
                                  MediaFailed="Player_MediaFailed"/>

                    <!-- no video overlay -->
                    <TextBlock x:Name="VideoPlaceholderText"
                               Text="Load video from the button →"
                               Foreground="#BDBDBD"
                               FontSize="20"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

        </Grid>
    </Border>
</UserControl>
