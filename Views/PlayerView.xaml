<UserControl x:Class="PlayCutWin.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="450"
             Background="Transparent">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   Text="Video (16:9)"
                   Foreground="#E6E6E6"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <!-- Team inputs -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Team A"
                           Foreground="#BDBDBD"
                           FontSize="14"
                           Margin="2,0,0,4"/>
                <TextBox x:Name="TeamATextBox"
                         Height="36"
                         Padding="12,6"
                         Background="#1F1F1F"
                         Foreground="#E6E6E6"
                         BorderBrush="#343434"
                         BorderThickness="1"
                         FontSize="16"
                         Text="{Binding TeamAName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         TextChanged="TeamATextBox_TextChanged"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Team B"
                           Foreground="#BDBDBD"
                           FontSize="14"
                           Margin="2,0,0,4"/>
                <TextBox x:Name="TeamBTextBox"
                         Height="36"
                         Padding="12,6"
                         Background="#1F1F1F"
                         Foreground="#E6E6E6"
                         BorderBrush="#343434"
                         BorderThickness="1"
                         FontSize="16"
                         Text="{Binding TeamBName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         TextChanged="TeamBTextBox_TextChanged"/>
            </StackPanel>
        </Grid>

        <!-- 16:9 Player -->
        <Border Grid.Row="2"
                Background="#2B2B2B"
                CornerRadius="10"
                BorderBrush="#3A3A3A"
                BorderThickness="1">
            <Grid>
                <Viewbox Stretch="Uniform">
                    <!-- 16:9 の固定キャンバス -->
                    <Grid Width="1600" Height="900" Background="Transparent">
                        <MediaElement x:Name="Player"
                                      LoadedBehavior="Manual"
                                      UnloadedBehavior="Manual"
                                      Stretch="Uniform"
                                      ScrubbingEnabled="True"/>

                        <!-- Placeholder -->
                        <TextBlock x:Name="PlaceholderText"
                                   Text="Load video from the button →"
                                   Foreground="#B5B5B5"
                                   FontSize="34"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Opacity="0.6"/>
                    </Grid>
                </Viewbox>
            </Grid>
        </Border>

    </Grid>
</UserControl>
