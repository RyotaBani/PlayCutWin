<UserControl x:Class="PlayCutWin.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Black">

    <Grid Margin="10">
        <Border Background="Black" CornerRadius="6" BorderBrush="#333" BorderThickness="1">
            <Grid>
                <TextBlock Text="PLAYER (placeholder)"
                           Foreground="#777"
                           FontSize="28"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="10">
                    <TextBlock Text="{Binding PlaybackPositionText}" Foreground="#DDD" FontSize="14"/>
                    <TextBlock Text=" / " Foreground="#777" FontSize="14"/>
                    <TextBlock Text="{Binding PlaybackDurationText}" Foreground="#777" FontSize="14"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
