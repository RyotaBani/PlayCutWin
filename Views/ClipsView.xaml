<UserControl x:Class="PlayCutWin.Views.ClipsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,10">
            <TextBlock Text="Imported Videos" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
            <TextBlock x:Name="CountText" Foreground="#B0B0B0" Margin="12,0,0,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="RightCount" Foreground="#B0B0B0" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
        </DockPanel>

        <Border Grid.Row="1" Background="#141414" BorderBrush="#2A2A2A" BorderThickness="1" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="(Import Video で選択したファイルがここに並ぶ)"
                           Foreground="#9A9A9A" FontSize="12" Margin="0,0,0,8"/>

                <DataGrid Grid.Row="1"
                          x:Name="VideosGrid"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          SelectionMode="Single"
                          SelectionChanged="VideosGrid_SelectionChanged"
                          Background="#0F0F0F"
                          Foreground="#EDEDED"
                          BorderBrush="#2A2A2A"
                          BorderThickness="1">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="300"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
