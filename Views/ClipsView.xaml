<UserControl x:Class="PlayCutWin.Views.ClipsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Imported Videos" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="#151515" CornerRadius="10" BorderBrush="#2A2A2A" BorderThickness="1" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="(Import Videoで選択したファイルがここに並ぶ)" Foreground="#AFAFAF" FontSize="12"/>
                    <TextBlock Text="{Binding ImportedVideos.Count, StringFormat=Count: {0}}"
                               Foreground="#AFAFAF" FontSize="12"
                               HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                </DockPanel>

                <DataGrid x:Name="VideosGrid"
                          Grid.Row="1"
                          ItemsSource="{Binding ImportedVideos}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          SelectionChanged="VideosGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
