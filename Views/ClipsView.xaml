<UserControl x:Class="PlayCutWin.Views.ClipsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Clips"
                   Foreground="#EEE"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Import" Width="100" Margin="0,0,8,0" Click="Import_Click"/>
            <Button Content="Export CSV" Width="120" Click="OpenExports_Click"/>
        </StackPanel>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding ImportedVideos}"
                 SelectedItem="{Binding SelectedVideo, Mode=TwoWay}"
                 DisplayMemberPath="Name"
                 Background="#111"
                 Foreground="#EEE"
                 BorderBrush="#333"
                 BorderThickness="1"
                 SelectionChanged="List_SelectionChanged"/>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Foreground="#999"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>
