<Window x:Class="PlayCutWin.Views.ExportProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exporting..." Height="180" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow"
        Background="#1F1F1F" Foreground="White">
  <Border Padding="16">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="12"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="10"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" Text="Exporting clips..."/>
      <ProgressBar x:Name="Bar" Grid.Row="2" Height="18" Minimum="0" Maximum="100" IsIndeterminate="True"/>
      <TextBlock x:Name="DetailText" Grid.Row="4" Opacity="0.9" TextWrapping="Wrap" Text="Preparing..."/>
    </Grid>
  </Border>
</Window>
