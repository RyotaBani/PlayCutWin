<UserControl x:Class="PlayCutWin.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">

    <Border Background="#1c1c1c" CornerRadius="12" Padding="16">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <CheckBox IsChecked="True" IsEnabled="False" Margin="0,0,8,0"/>
                <TextBlock Text="Windows build is working" Foreground="White" FontWeight="Bold" FontSize="16"/>
            </StackPanel>

            <TextBlock Text="Next: replace this area with real PlayCut UI (timeline / tags / clip list)."
                       Foreground="#bdbdbd" Margin="0,0,0,14"/>

            <TextBlock Text="{Binding SelectedVideoPath}" Foreground="#d0d0d0" TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
