<UserControl x:Class="PlayCutWin.Views.PlayerControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PlayCutWin"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">

    <!-- ★重要：Resourcesを置かない（key=null系を根絶） -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ===== Playback Speed ===== -->
        <TextBlock Grid.Row="0" Text="Playback Speed" Style="{StaticResource Small}" Margin="0,0,0,6"/>

        <UniformGrid Grid.Row="1" Columns="4" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="0.25x" Style="{StaticResource TopButton}" Click="Speed025_Click"/>
            <Button Content="0.5x"  Style="{StaticResource TopButton}" Click="Speed05_Click"/>
            <Button Content="1x"    Style="{StaticResource AccentButton}" Click="Speed1_Click"/>
            <Button Content="2x"    Style="{StaticResource TopButton}" Click="Speed2_Click"/>
        </UniformGrid>

        <!-- ===== Seek / Play / Time ===== -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- -5s -->
            <Button Grid.Column="0" Content="-5s" Style="{StaticResource TopButton}" Click="SeekMinus5_Click"/>

            <!-- -1s -->
            <Button Grid.Column="1" Content="-1s" Style="{StaticResource TopButton}" Click="SeekMinus1_Click"/>

            <!-- Play/Pause (★サイズを他と完全統一) -->
            <Button Grid.Column="2" Style="{StaticResource AccentButton}" Click="PlayPause_Click">
                <Button.Content>
                    <TextBlock FontFamily="Segoe MDL2 Assets"
                               FontSize="{DynamicResource FontBody}"
                               Foreground="{DynamicResource Text1}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <!-- ▶ -->
                                <Setter Property="Text" Value="&#xE768;"/>
                                <Style.Triggers>
                                    <!-- ⏸ -->
                                    <DataTrigger Binding="{Binding IsPlaying, Source={x:Static local:AppState.Instance}}" Value="True">
                                        <Setter Property="Text" Value="&#xE769;"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Button.Content>
            </Button>

            <!-- +1s -->
            <Button Grid.Column="3" Content="+1s" Style="{StaticResource TopButton}" Click="SeekPlus1_Click"/>

            <!-- +5s -->
            <Button Grid.Column="4" Content="+5s" Style="{StaticResource TopButton}" Click="SeekPlus5_Click"/>

            <!-- time text -->
            <TextBlock Grid.Column="5"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Style="{StaticResource Body}"
                       Text="{Binding PlaybackPositionText, Source={x:Static local:AppState.Instance}}">
                <TextBlock.Inlines>
                    <Run Text="  /  "/>
                    <Run Text="{Binding PlaybackDurationTextLong, Source={x:Static local:AppState.Instance}}"/>
                </TextBlock.Inlines>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>
