<UserControl x:Class="PlayCutWin.Views.ExportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#1c1c1c" CornerRadius="12" Padding="14">
            <StackPanel>
                <TextBlock Text="Selected video:" Foreground="#bdbdbd"/>
                <TextBlock x:Name="SelectedVideoText" Foreground="White" TextWrapping="Wrap" Margin="0,2,0,8"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="Choose Folder (via SaveDialog)" Width="220" Margin="0,0,10,0" Click="ChooseFolder_Click"/>
                    <TextBlock x:Name="SelectedFolderText" Foreground="#d0d0d0" VerticalAlignment="Center" Text="(not set)"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#1c1c1c" CornerRadius="12" Padding="14">
            <StackPanel>
                <TextBlock Text="(placeholder) export selected clip / tags list"
                           Foreground="#bdbdbd" Margin="0,0,0,10"/>

                <Button Content="Export (dummy)"
                        Width="160" Height="34"
                        Click="ExportDummy_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
