<UserControl x:Class="PlayCutWin.Views.ExportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Border Background="#0F0F0F" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Exports" Foreground="White" FontSize="18" FontWeight="SemiBold"/>

                <TextBlock Grid.Row="1" Margin="0,10,0,6"
                           Foreground="#BDBDBD"
                           Text="{Binding SelectedVideoPath, StringFormat=Selected video: {0}}"/>

                <StackPanel Grid.Row="2" VerticalAlignment="Top" Margin="0,18,0,0">
                    <Button Content="Choose Folder (dummy)" Width="220" Height="34" Margin="0,0,0,10"
                            Click="ChooseFolder_Click"/>
                    <TextBlock x:Name="FolderText" Foreground="#CFCFCF" Margin="0,0,0,16"/>
                    <Button Content="Export Selected Clip (dummy)" Width="260" Height="38"
                            Click="ExportSelected_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
