<UserControl x:Class="PlayCutWin.Views.ExportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1A1A1A">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Text="Exports"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="#EEE"
                   Margin="0,0,0,12"/>

        <!-- 選択中クリップ -->
        <TextBlock Grid.Row="1"
                   Text="{Binding SelectedVideoPath, FallbackValue=No clip selected}"
                   Foreground="#AAA"
                   FontSize="12"
                   Margin="0,0,0,12"/>

        <!-- ボタン -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0,0,0,12">

            <Button Content="Export Selected"
                    Width="140"
                    Margin="0,0,12,0"
                    Click="ExportSelected_Click"/>

            <Button Content="Export All"
                    Width="120"
                    Click="ExportAll_Click"/>
        </StackPanel>

        <!-- 説明 -->
        <TextBlock Grid.Row="3"
                   Text="CSV形式でタグ付きクリップを書き出します。"
                   Foreground="#888"
                   FontSize="12"/>
    </Grid>
</UserControl>
