<UserControl x:Class="PlayCutWin.Views.ExportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Border CornerRadius="12" Background="#2B2B2B" BorderBrush="#3A3A3A" BorderThickness="1" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Exports"
                           Foreground="#EDEDED"
                           FontSize="16"
                           FontWeight="SemiBold"/>

                <TextBlock Grid.Row="1"
                           Text="{Binding SelectedVideoPath}"
                           Foreground="#CFCFCF"
                           FontSize="12"
                           TextTrimming="CharacterEllipsis"
                           Margin="0,8,0,10"/>

                <StackPanel Grid.Row="2" VerticalAlignment="Top" Margin="0,6,0,0">
                    <WrapPanel>
                        <Button Content="Import CSV" Padding="16,6" Margin="0,0,8,8" Click="ImportCsv_Click"/>
                        <Button Content="Export CSV" Padding="16,6" Margin="0,0,8,8" Click="ExportCsv_Click"/>
                        <Button Content="Export All (dummy)" Padding="16,6" Margin="0,0,8,8" Click="ExportAll_Click"/>
                    </WrapPanel>

                    <TextBlock Margin="0,6,0,0"
                               Foreground="#BDBDBD"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Text="{Binding StatusMessage}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
