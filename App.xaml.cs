using System.Windows;

namespace PlayCutWin
{
    public partial class App : Application
    {
        // StartupUri は App.xaml に指定。
        // ここで MainWindow を手動で Show すると二重起動の原因になるので空にしておく。
    }
}
