<Window x:Class="PlayCutWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlayCutWin"
        mc:Ignorable="d"
        Title="PlayCutWin" Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#111111">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="14"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <!-- =========================
             LEFT: Video Player
        ========================== -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Top bar -->
            <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,10">
                <TextBlock Text="Player" Foreground="#EAEAEA" FontSize="18" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button x:Name="BtnLoadVideo" Content="Load Video" Margin="0,0,8,0" Padding="12,6"
                            Click="LoadVideo_Click"/>
                    <Button x:Name="BtnPlay" Content="Play" Margin="0,0,8,0" Padding="12,6"
                            Click="Play_Click"/>
                    <Button x:Name="BtnPause" Content="Pause" Padding="12,6"
                            Click="Pause_Click"/>
                </StackPanel>
            </DockPanel>

            <!-- Video area (FIX: CornerRadius is on Border, not Grid) -->
            <Border Grid.Row="1" Background="Black" CornerRadius="10">
                <Grid>
                    <MediaElement x:Name="Player"
                                  LoadedBehavior="Manual"
                                  UnloadedBehavior="Manual"
                                  Stretch="Uniform"
                                  MediaOpened="Player_MediaOpened"
                                  MediaFailed="Player_MediaFailed"/>
                    <TextBlock x:Name="VideoHint" Text="No video loaded" Foreground="#AAAAAA"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="18" FontWeight="SemiBold" Opacity="0.85"/>
                </Grid>
            </Border>

            <!-- Timeline -->
            <StackPanel Grid.Row="3" Orientation="Vertical">
                <Slider x:Name="TimelineSlider" Minimum="0" Maximum="100" Value="0"
                        PreviewMouseLeftButtonDown="Timeline_PreviewMouseLeftButtonDown"
                        PreviewMouseLeftButtonUp="Timeline_PreviewMouseLeftButtonUp"
                        ValueChanged="Timeline_ValueChanged"/>
                <DockPanel Margin="0,6,0,0">
                    <TextBlock x:Name="TxtTime" Text="00:00 / 00:00" Foreground="#CFCFCF"/>
                    <TextBlock x:Name="StatusText" Text="" Foreground="#88DDAA" DockPanel.Dock="Right"/>
                </DockPanel>
            </StackPanel>
        </Grid>

        <!-- =========================
             RIGHT: Clip List + Tags
        ========================== -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Clips" Foreground="#EAEAEA" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

            <ListView x:Name="ClipList" Grid.Row="1"
                      SelectionChanged="ClipList_SelectionChanged"
                      MouseDoubleClick="ClipList_DoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Start" Width="90" DisplayMemberBinding="{Binding Start}"/>
                        <GridViewColumn Header="End" Width="90" DisplayMemberBinding="{Binding End}"/>
                        <GridViewColumn Header="Tags" Width="260" DisplayMemberBinding="{Binding TagsText}"/>
                        <GridViewColumn Header="Team" Width="90" DisplayMemberBinding="{Binding Team}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Add Clip" Padding="12,6" Margin="0,0,8,0" Click="AddClip_Click"/>
                <Button Content="Delete" Padding="12,6" Click="DeleteSelectedClip_Click"/>
            </StackPanel>

            <TextBlock Grid.Row="5" Text="(Double click a clip to Jump)" Foreground="#AAAAAA" Opacity="0.9"/>
        </Grid>
    </Grid>
</Window>
