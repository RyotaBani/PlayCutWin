<Window x:Class="PlayCutWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Play Cut"
        Width="1400" Height="860"
        Background="#151515"
        Foreground="#EAEAEA">

    <Window.Resources>

        <!-- ===== Watermark TextBox ===== -->
        <Style x:Key="WatermarkTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="Background" Value="#262626"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"/>
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>

                            <!-- Placeholder -->
                            <TextBlock Text="{TemplateBinding Tag}"
                                       Foreground="#8C8C8C"
                                       Margin="{TemplateBinding Padding}"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                         Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
            <Setter Property="Background" Value="#0A84FF"/>
            <Setter Property="BorderBrush" Value="#0A84FF"/>
        </Style>

    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.3*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= LEFT ================= -->
        <Border Grid.Column="0" Padding="16" CornerRadius="14" Background="#1B1B1B">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel LastChildFill="False" Margin="0,0,0,12">

                    <!-- Right buttons -->
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Button Style="{StaticResource Btn}" Content="Import CSV" Click="ImportCSV_Click"/>
                        <Button Style="{StaticResource Btn}" Content="Export CSV" Click="ExportCSV_Click"/>
                        <Button Style="{StaticResource Btn}" Content="Export All" Click="ExportAll_Click"/>
                        <Button Style="{StaticResource PrimaryBtn}" Content="Load Video" Click="LoadVideo_Click"/>
                    </StackPanel>

                    <!-- Title + Team -->
                    <StackPanel Width="700">
                        <TextBlock Text="{Binding VideoPanelTitle}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,0,0,6"/>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Team A" Margin="0,0,8,0"/>
                            <TextBox Width="220"
                                     Style="{StaticResource WatermarkTextBox}"
                                     Tag="Home / Our Team"
                                     Text="{Binding TeamAName}"
                                     GotKeyboardFocus="TextBox_SelectAllOnFocus"
                                     PreviewMouseLeftButtonDown="TextBox_PreviewMouseLeftButtonDown_SelectAll"/>

                            <TextBlock Text="Team B" Margin="16,0,8,0"/>
                            <TextBox Width="220"
                                     Style="{StaticResource WatermarkTextBox}"
                                     Tag="Away / Opponent"
                                     Text="{Binding TeamBName}"
                                     GotKeyboardFocus="TextBox_SelectAllOnFocus"
                                     PreviewMouseLeftButtonDown="TextBox_PreviewMouseLeftButtonDown_SelectAll"/>
                        </StackPanel>
                    </StackPanel>

                </DockPanel>

                <!-- Player -->
                <Border Grid.Row="1" Background="#000000" CornerRadius="12"/>
            </Grid>
        </Border>

        <!-- ================= RIGHT ================= -->
        <Border Grid.Column="1" Margin="16,0,0,0" Padding="16" CornerRadius="14" Background="#1B1B1B">
            <TextBlock Text="Clips" FontSize="16" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</Window>
